import React from 'react';
import { Helmet } from 'react-helmet';

export default function EmployeesPage() {
    return (
        <div>
            <Helmet title={'Starsky | Employees'} />
            <h1>Employees</h1>
        </div>
    );
}
